# lib/cadence_backend/cadence_backend.ex
defmodule CadenceBackend do
  @moduledoc """
  The CadenceBackend context.
  """

  # Este arquivo geralmente permanece minimalista ou contém funções gerais
  # da aplicação que não são específicas da interface web (CadenceBackendWeb).
  # Por exemplo: transformações de dados, lógica de domínio não atrelada a um contexto específico.
  # Por enquanto, ele pode conter apenas a definição do módulo.
end
